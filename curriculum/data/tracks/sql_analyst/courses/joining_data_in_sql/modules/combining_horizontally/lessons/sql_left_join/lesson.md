# Keeping All Rows with LEFT JOIN

Enrich a base population without losing unmatched rows.

## What you will learn

- Explain keeping all rows with left join.
- Apply keeping all rows with left join to an analyst request.

## Main idea

In this step, you will use **anti-join pattern** inside a complete analyst query. Start by identifying the result grain—what one row should represent—then select only the fields and calculations needed to answer the request. Run the query, inspect the output, and check the answer when the result looks right.

## Key takeaways

- Enrich a base population without losing unmatched rows.
- Validate the result rather than relying only on valid syntax.
