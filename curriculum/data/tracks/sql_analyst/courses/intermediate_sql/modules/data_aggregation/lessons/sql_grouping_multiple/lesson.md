# Multiple Grouping Columns

Break summaries across more than one business dimension.

## What you will learn

- Explain multiple grouping columns.
- Apply multiple grouping columns to an analyst request.

## Main idea

In this step, you will use **multiple grouping columns** inside a complete analyst query. Start by identifying the result grain—what one row should represent—then select only the fields and calculations needed to answer the request. Run the query, inspect the output, and check the answer when the result looks right.

## Key takeaways

- Break summaries across more than one business dimension.
- Validate the result rather than relying only on valid syntax.
