# Working with DATE/TIME Functions and Operators

Extract date parts, truncate periods, and calculate elapsed time.

## What you will learn

- Explain working with date/time functions and operators.
- Apply working with date/time functions and operators to an analyst request.

## Main idea

In this step, you will use **date period truncation** inside a complete analyst query. Start by identifying the result grain—what one row should represent—then select only the fields and calculations needed to answer the request. Run the query, inspect the output, and check the answer when the result looks right.

## Key takeaways

- Extract date parts, truncate periods, and calculate elapsed time.
- Validate the result rather than relying only on valid syntax.
