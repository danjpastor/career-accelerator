# Beyond Window Functions

Create pivot-like summaries and subtotal levels for reporting.

## What you will learn

- Explain beyond window functions.
- Apply beyond window functions to an analyst request.

## Main idea

In this step, you will use **subtotal grouping sets** inside a complete analyst query. Start by identifying the result grain—what one row should represent—then select only the fields and calculations needed to answer the request. Run the query, inspect the output, and check the answer when the result looks right.

## Key takeaways

- Create pivot-like summaries and subtotal levels for reporting.
- Validate the result rather than relying only on valid syntax.
