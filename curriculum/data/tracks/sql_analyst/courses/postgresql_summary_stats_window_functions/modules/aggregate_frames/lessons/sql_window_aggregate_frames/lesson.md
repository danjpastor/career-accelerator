# Aggregate Window Functions and Frames

Create running and moving metrics with explicit window frames.

## What you will learn

- Explain aggregate window functions and frames.
- Apply aggregate window functions and frames to an analyst request.

## Main idea

In this step, you will use **moving averages** inside a complete analyst query. Start by identifying the result grain—what one row should represent—then select only the fields and calculations needed to answer the request. Run the query, inspect the output, and check the answer when the result looks right.

## Key takeaways

- Create running and moving metrics with explicit window frames.
- Validate the result rather than relying only on valid syntax.
