# VFX Production Intelligence Dashboard — Data Dictionary

Generated from the reviewed Data Dictionary Studio on 2026-07-24 12:21.

- Tables: 6
- Fields: 84

## Artists (`artists`)

**Description:** Contains records for artists used by the approved project analysis, including artist display name, primary VFX department, and production role.

**Grain:** One row per artist

**Expected primary key:** `artist_id`

**Relationships:**

- shots.assigned_artist_id → artists.artist_id
- time_entries.artist_id → artists.artist_id

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `artist_id` | VARCHAR | Identifier text | Unique identifier assigned to each artist record in the production workforce. | No nulls allowed | Primary key | Required | ART-###: uppercase ART, a hyphen, and three digits. |  |  | artist_id remains the intended primary key. The repeated IDs are raw-data defects that will be removed during cleaning. | Remove exact duplicate artist rows, retain one canonical record per ID, trim whitespace, standardize casing, and verify that the cleaned key is unique and non-null. |
| `artist_name` | VARCHAR | Text | Full name used to identify the fictional artist in staffing and reporting views. | No nulls allowed | Not a key | Not required | First and last name in consistent display casing. |  |  | Names are descriptive and may legitimately repeat; artist_id is the authoritative identifier. | Trim surrounding whitespace and standardize display casing without treating repeated names as duplicate artists. |
| `department` | VARCHAR | Category text | Primary VFX production department to which the artist is assigned. | No nulls allowed | Not a key | Not required | Animation; Assets; Compositing; FX; Lighting; Matchmove; Matte Painting; Roto/Paint. |  |  | The raw text variants represent the same controlled department categories and will be standardized in the cleaned table. | Trim whitespace and map all variants to the approved department labels. |
| `role` | VARCHAR | Category text | Artist’s primary production role or discipline within the assigned department. | No nulls allowed | Not a key | Not required | Animator; Senior Animator; Compositor; Senior Compositor; FX Artist; Senior FX Artist; FX Lead; Lighting Artist; Senior Lighting Artist; Matchmove Artist; Matchmove Lead; Tracking Artist; Matte Painter; Modeler; Texture Artist; Paint Artist; Roto Artist. |  |  |  | Trim whitespace and preserve the approved production-role labels. |
| `seniority` | VARCHAR | Category text | Artist experience level used for staffing, capacity, and labor-cost analysis. | No nulls allowed | Not a key | Not required | Junior; Mid; Senior; Lead. |  |  |  | Standardize capitalization to the approved seniority labels. |
| `location` | VARCHAR | Category text | Artist’s assigned office location or remote-work region. | No nulls allowed | Not a key | Not required | Atlanta; London; Los Angeles; Montreal; Vancouver; Remote-US; Remote-EU. |  |  |  | Trim whitespace and standardize values to the approved location labels. |
| `weekly_capacity_hours` | VARCHAR | Decimal | Number of hours the artist is normally available for schedulable production work each week. | No nulls allowed | Not a key | Not required | Non-negative numeric hours; zero is allowed only when the artist has no schedulable production capacity. |  | Hours per week | The field is logically numeric even though dirty source values caused it to load as text. Invalid or unusual capacities will be corrected or documented before utilization analysis. | Remove text suffixes, convert to decimal, investigate negative and extreme values, and preserve zero only when supported by the artist’s current work status. |
| `hourly_cost_usd` | VARCHAR | Currency / decimal | Fictional internal hourly labor-cost rate used to estimate artist and project labor cost. | No nulls allowed | Not a key | Not required | Positive numeric amount without currency symbols or text suffixes. |  | USD per hour | The field is expected to be numeric currency; raw formatting, a blank, and a negative value explain the observed text type and require cleaning. | Remove formatting characters, convert to decimal, fill only business-confirmed missing values, and investigate or correct non-positive rates before cost analysis. |
| `hire_date` | DATE | Date | Date the artist began employment with the fictional VFX studio. | No nulls allowed | Not a key | Not required | YYYY-MM-DD. |  |  |  | Preserve as a date and validate that hire dates precede related production activity. |
| `active_flag` | VARCHAR | Boolean | Indicates whether the artist is currently active in the studio workforce. | No nulls allowed | Not a key | Not required | Y = active; N = inactive. |  |  | The source stores one boolean concept with several text encodings. The cleaned field will use only Y and N. | Standardize affirmative values to Y and negative values to N; send unrecognized values for review. |
| `manager_id` | VARCHAR | Identifier text | Identifier of the artist’s direct manager, department lead, or other reporting supervisor. | Conditionally nullable | Self-referencing foreign key | Not required | ART-###, or null when no manager is represented in the dataset. | artists.artist_id |  | Nulls are valid only for artists with no manager represented in the dataset; populated values remain required to match an artist record. | Preserve legitimate top-level nulls, trim and standardize populated IDs, and validate every non-null value against artists.artist_id. |
| `email` | VARCHAR | Text | Synthetic, non-routable company email address assigned to the artist. | No nulls allowed | Not a key | Required | first_name.last_name@spectraforgevfx.example. |  |  | Email is expected to be required and unique, but the raw export contains missing and repeated values that must be resolved during cleaning. | Fill business-confirmed missing addresses, correct misassigned addresses, remove exact duplicate rows, and apply an approved unique-alias rule to unresolved same-name collisions. |

## Clients (`clients`)

**Description:** Contains records for clients used by the approved project analysis, including client or studio name, business category such as feature studio or agency, and primary client region.

**Grain:** One row per client

**Expected primary key:** `client_id`

**Relationships:**

- projects.client_id → clients.client_id

**Table notes:** clients.client_id is the parent of projects.client_id. Before the relationship can be used in final analysis, remove duplicate client rows, trim the whitespace from project PRJ-1003’s client_id, and resolve or quarantine project PRJ-1011, which references nonexistent client CL-999.

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `client_id` | VARCHAR | Identifier text | Unique identifier assigned to each client account. | No nulls allowed | Primary key | Required | CL-###: uppercase CL, a hyphen, and three digits. |  |  | client_id remains the intended primary key. Repeated IDs are duplicate raw records rather than a data-model decision. | Remove exact duplicate rows, retain one canonical record per ID, trim whitespace, standardize casing, and verify uniqueness and completeness. |
| `client_name` | VARCHAR | Text | Official display name of the fictional client, studio, agency, platform, or vendor commissioning work. | No nulls allowed | Not a key | Not required | Trimmed organization name in consistent display casing. |  |  | Some raw names contain surrounding whitespace or all-uppercase formatting. | Trim whitespace and standardize display casing while preserving the intended organization name. |
| `client_type` | VARCHAR | Category text | Business category describing the type of organization represented by the client account. | No nulls allowed | Not a key | Not required | Agency; Feature Studio; Game Cinematics; Independent Studio; Marketing Vendor; Streaming Platform; Television Studio. |  |  |  | Trim whitespace and standardize to the approved client-type labels. |
| `region` | VARCHAR | Category text | Primary geographic operating region associated with the client account. | No nulls allowed | Not a key | Not required | North America; Europe; Asia-Pacific. |  |  | N. America is an alternate label for North America and should not remain as a separate reporting category. | Map N. America to North America, trim whitespace, and retain the three approved regions. |
| `contract_tier` | VARCHAR | Category text | Commercial service tier assigned to the client for account segmentation and service planning. | No nulls allowed | Not a key | Not required | A; B; C. |  |  |  | Trim whitespace, standardize to uppercase, and investigate values outside A, B, or C. |
| `revision_tendency_index` | DOUBLE | Decimal | Relative multiplier used to model the client’s expected tendency to generate revision activity. | No nulls allowed | Not a key | Not required | Positive decimal; 1.0 is the baseline, above 1.0 is higher tendency, and below 1.0 is lower tendency. |  | Relative index multiplier |  | Preserve valid positive decimal values and investigate null, zero, or negative values. |
| `default_review_sla_hours` | BIGINT | Integer | Default number of hours expected for the client to complete or respond to a production review. | No nulls allowed | Not a key | Not required | Positive whole number. |  | Hours |  | Preserve valid positive integers and investigate missing, zero, negative, or fractional values. |
| `active_flag` | VARCHAR | Boolean | Indicates whether the client relationship is currently active. | No nulls allowed | Not a key | Not required | Y = active; N = inactive. |  |  | The raw text field uses several encodings for one boolean concept. The cleaned field will use only Y and N. | Standardize affirmative values to Y and negative equivalents to N; investigate unrecognized values. |
| `account_manager` | VARCHAR | Text | Internal account owner responsible for managing the studio’s relationship with the client. | No nulls allowed | Not a key | Not required | Person name in First Last format. |  |  | Repeated manager names are valid because one account manager may own multiple client relationships. | Trim whitespace and standardize name presentation without treating legitimate repetition as duplicate data. |
| `notes` | VARCHAR | Text | Optional free-form account notes containing client-specific context, exceptions, or operational information. | Nulls allowed | Not a key | Not required | Free-form text, or null when no account note is recorded. |  |  | The high null rate is expected for an optional notes field and does not require imputation. | Trim populated notes and convert empty strings to null. |

## Projects (`projects`)

**Description:** Stores project-level production information used to analyze client work, schedules, delivery performance, planned scope, budgeted labor, production leadership, and technical delivery requirements.

**Grain:** One VFX production project.

**Expected primary key:** `project_id`

**Relationships:**

- projects.client_id → clients.client_id
- reviews.project_id → projects.project_id
- shots.project_id → projects.project_id
- time_entries.project_id → projects.project_id

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `project_id` | VARCHAR | Identifier text | Unique identifier assigned to each VFX production project. | No nulls allowed | Primary key | Required | PRJ-####: uppercase PRJ, a hyphen, and four digits. |  |  | project_id remains the intended primary key. The repeated value is an exact duplicate raw record. | Remove the exact duplicate row, retain one canonical project record, standardize the identifier, and verify uniqueness and completeness. |
| `client_id` | VARCHAR | Identifier text | Identifier of the client account that commissioned or owns the production project. | No nulls allowed | Foreign key | Not required | CL-###: uppercase CL, a hyphen, and three digits. | clients.client_id |  | client_id remains a required foreign key. The whitespace variant is recoverable; CL-999 must be corrected or quarantined. | Trim and standardize identifiers; correct only confirmed client assignments and quarantine unresolved orphan references from relationship-dependent analysis. |
| `project_name` | VARCHAR | Text | Business-facing title used to identify the production project. | No nulls allowed | Not a key | Not required | Trimmed project title in consistent display casing. |  |  | Starforge Cinematic repeats only because the complete PRJ-1005 row is duplicated. | Trim whitespace and remove repeated names only as part of exact-row deduplication; project_id remains the identifier. |
| `project_type` | VARCHAR | Category text | Category describing the project’s production format or type of work. | No nulls allowed | Not a key | Not required | Commercial; Episodic; Feature Film; Game Cinematic; Independent Feature; Streaming Series; Trailer. |  |  |  | Trim whitespace and standardize to the approved project-type labels. |
| `start_date` | VARCHAR | Date | Date on which production work for the project officially began or was scheduled to begin. | No nulls allowed | Not a key | Not required | YYYY-MM-DD. |  |  | The field is logically a date; mixed source formats explain the observed text type and will be standardized. | Parse valid source formats, store as a true date, and display consistently as YYYY-MM-DD. |
| `target_delivery_date` | DATE | Date | Planned date by which the project is expected to be delivered to the client. | No nulls allowed | Not a key | Not required | YYYY-MM-DD and on or after start_date. |  |  | The date-order violation is a raw-data defect and must be corrected before schedule analysis. | Preserve valid dates, investigate and correct the impossible sequence, and validate target_delivery_date >= start_date. |
| `actual_delivery_date` | VARCHAR | Date | Date on which the completed project was actually delivered to the client. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null while the project has not been delivered. |  |  | Nulls are valid for unfinished projects; mixed date formats explain the observed text type and will be standardized. | Parse valid dates, preserve legitimate nulls for unfinished projects, and require a date when the standardized status is Completed. |
| `status` | VARCHAR | Category text | Current lifecycle stage of the production project. | No nulls allowed | Not a key | Not required | Active; In Progress; In Delivery; Completed. |  |  | Multiple raw labels represent the same project states and will be standardized. | Trim whitespace and map variants to the four approved lifecycle statuses. |
| `priority` | VARCHAR | Category text | Production priority assigned to the project for scheduling and resource-planning decisions. | No nulls allowed | Not a key | Not required | Medium; High; Critical. |  |  |  | Trim whitespace and standardize capitalization to the approved values. |
| `planned_shot_count` | BIGINT | Integer | Number of VFX shots planned within the project’s approved production scope. | No nulls allowed | Not a key | Not required | Positive whole number. |  | Shots |  | Preserve valid positive integers and investigate missing, zero, negative, or fractional values. |
| `budget_hours` | VARCHAR | Decimal | Total labor hours budgeted for completion of the project. | No nulls allowed | Not a key | Not required | Positive numeric value. |  | Labor hours | The field is expected to be numeric and required; raw formatting, a blank, and a negative value explain the text type and require remediation. | Remove formatting text, convert to decimal, obtain confirmed replacements for missing or invalid budgets, and reject non-positive values before analysis. |
| `producer` | VARCHAR | Text | Producer responsible for coordinating the project schedule, staffing, client communication, and delivery. | No nulls allowed | Not a key | Not required | Person name in First Last format. |  |  | Repeated names are valid because one producer may oversee multiple projects. | Trim whitespace and standardize name presentation while preserving legitimate repetition. |
| `vfx_supervisor` | VARCHAR | Text | VFX supervisor responsible for the project’s creative and technical visual-effects oversight. | No nulls allowed | Not a key | Not required | Recorded person name, commonly initial and surname. |  |  | Repeated names are valid because one supervisor may oversee multiple projects. | Trim whitespace and preserve the established name format. |
| `fps` | DOUBLE | Decimal | Frame rate at which the project is produced and delivered. | No nulls allowed | Not a key | Not required | 23.976; 24; 25; 29.97. |  | Frames per second |  | Preserve valid positive frame rates as decimals and investigate unsupported values. |
| `resolution` | VARCHAR | Category text | Target image resolution and framing specification for project delivery. | No nulls allowed | Not a key | Not required | 2K Flat; 2K Scope; 4K DCI; 4K UHD. |  |  |  | Trim whitespace and standardize to the approved resolution labels. |
| `delivery_format` | VARCHAR | Category text | File format and bit depth required for the project’s final image delivery. | No nulls allowed | Not a key | Not required | DPX 10-bit; EXR 16-bit; EXR 32-bit; ProRes 4444. |  |  |  | Trim whitespace and standardize to the approved delivery-format labels. |

## Reviews (`reviews`)

**Description:** Stores shot-review events used to analyze revision activity, approval outcomes, internal-versus-client feedback, review turnaround time, and review-related production risk.

**Grain:** One review event for one shot.

**Expected primary key:** `review_id`

**Relationships:**

- reviews.project_id → projects.project_id
- reviews.shot_id → shots.shot_id

**Table notes:** The raw Reviews table contains 3,883 rows and 3,843 distinct review IDs. It includes duplicate review records, invalid or missing shot and project references, mixed and invalid review dates, invalid review-round values, inconsistent outcomes, and invalid response-hour values.

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `review_id` | VARCHAR | Identifier text | Unique identifier assigned to each recorded shot-review event. | No nulls allowed | Primary key | Required | REV-#######: uppercase REV, a hyphen, and seven digits. |  |  | review_id remains the intended primary key. Repeated IDs are raw-data defects rather than a change to the event grain. | Remove exact duplicate review rows, quarantine conflicting repeated IDs, and verify that the cleaned key is unique and non-null. |
| `shot_id` | VARCHAR | Identifier text | Identifier of the production shot evaluated during the review event. | No nulls allowed | Foreign key | Not required | PRJ-####_SQ###_SH####. | shots.shot_id |  | shot_id remains a required foreign key; unresolved review-to-shot references must be excluded from joined review analysis. | Trim and uppercase valid IDs, correct only confidently recoverable values, and quarantine null, placeholder, or unresolved references. |
| `project_id` | VARCHAR | Identifier text | Identifier of the production project associated with the reviewed shot. | No nulls allowed | Foreign key | Not required | PRJ-####. | projects.project_id |  | project_id remains a required foreign key and must agree with both the Projects table and the project inherited through shot_id. | Trim and uppercase IDs, validate them against projects.project_id, reconcile each value to the related shot’s project, and quarantine unresolved contradictions. |
| `review_date` | VARCHAR | Date | Calendar date on which review feedback was recorded. | No nulls allowed | Not a key | Not required | YYYY-MM-DD. |  |  | The field is logically a required date; malformed source values explain the text type and require cleaning. | Parse valid formats, store as a true date, and quarantine blank, placeholder, or impossible values that cannot be corrected reliably. |
| `review_round` | VARCHAR | Integer | Sequential number representing the review round reached by the shot. | No nulls allowed | Not a key | Not required | Positive whole number beginning at 1. |  | Review round | The field is expected to be an integer; malformed source labels and invalid values explain the observed text type. | Convert confidently interpretable values to positive integers, validate sequence within each shot, and quarantine unresolved or non-positive values. |
| `review_type` | VARCHAR | Category text | Indicates whether the review was conducted internally or by a client-side reviewer. | No nulls allowed | Not a key | Not required | Internal Review; Client Review. |  |  |  | Trim whitespace and standardize to the two approved review types. |
| `feedback_source` | VARCHAR | Category text | Role or stakeholder group responsible for supplying the review feedback. | No nulls allowed | Not a key | Not required | Department Lead; Comp Supervisor; VFX Supervisor; Client Supervisor; Agency Creative. |  |  | Repeated values are expected because the same role category applies to many review events. | Trim whitespace and standardize capitalization without combining genuinely different feedback-source roles. |
| `outcome` | VARCHAR | Category text | Result assigned to the review event, indicating acceptance, requested changes, notes, or a hold. | No nulls allowed | Not a key | Not required | Approved; Approved with Notes; Changes Requested; Minor Notes; Hold. |  |  | Multiple raw labels represent the same review outcomes and will be standardized for consistent approval and revision metrics. | Trim whitespace and map known variants to the five approved outcomes; investigate unfamiliar values before mapping. |
| `note_category` | VARCHAR | Category text | Primary category describing the type of feedback or correction recorded during the review. | No nulls allowed | Not a key | Not required | Color; Technical; Integration; Client Preference; Timing; Editorial Change; Edge Work; Continuity. |  |  |  | Trim whitespace and standardize to the approved feedback categories; validate logical consistency with the review outcome. |
| `response_hours` | VARCHAR | Decimal | Number of elapsed hours until review feedback was acknowledged or addressed. | No nulls allowed | Not a key | Not required | Non-negative decimal value. |  | Hours | The field is expected to be numeric and non-negative; malformed and extreme source values explain the text type and require remediation. | Convert valid values to decimal, reject negatives, investigate blanks and extreme values against review context and client SLA, and exclude unresolved values from averages. |
| `reviewer` | VARCHAR | Text | Name of the individual recorded as conducting or communicating the review. | No nulls allowed | Not a key | Not required | Recorded person name, either full name or initial and surname. |  |  | Repeated reviewer names are expected because one person may review many shots. | Trim whitespace and standardize display formatting where the same person can be identified confidently. |
| `notes` | VARCHAR | Text | Optional free-form text providing additional context about review feedback or requested changes. | Nulls allowed | Not a key | Not required | Free-form text, or null when no additional comment is recorded. |  |  | Missing notes and repeated common instructions are expected and do not by themselves indicate duplicate reviews. | Trim populated text, convert empty strings to null, and preserve the original wording. |

## Shots (`shots`)

**Description:** Contains records for shots used by the approved project analysis, including owning project, editorial sequence identifier, and shot code within a project.

**Grain:** One row per shot

**Expected primary key:** `shot_id`

**Relationships:**

- reviews.shot_id → shots.shot_id
- shots.assigned_artist_id → artists.artist_id
- shots.project_id → projects.project_id
- time_entries.shot_id → shots.shot_id

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `shot_id` | VARCHAR | Identifier text | Unique identifier combining the owning project, editorial sequence, and shot code. | No nulls allowed | Primary key | Required | PRJ-####_SQ###_SH####. |  |  | shot_id remains the intended primary key. Repeated IDs are raw-data defects rather than a change to the one-shot grain. | Remove exact duplicate rows, quarantine conflicting repeated IDs, standardize casing and whitespace, and verify uniqueness and completeness. |
| `project_id` | VARCHAR | Identifier text | Identifier of the project that owns the production shot. | No nulls allowed | Foreign key | Not required | PRJ-####. | projects.project_id |  | project_id remains a required foreign key. Recoverable formatting issues will be standardized; nonexistent or contradictory values require correction or quarantine. | Trim and uppercase identifiers, validate against projects.project_id, reconcile the project prefix in shot_id, and quarantine unresolved references. |
| `sequence_code` | VARCHAR | Identifier text | Editorial sequence identifier within the owning project. | No nulls allowed | Not a key | Not required | SQ###: uppercase SQ followed by three digits. |  |  |  | Trim whitespace, standardize casing, and verify that the value matches the sequence portion of shot_id. |
| `shot_name` | VARCHAR | Identifier text | Shot code used to identify the shot within its project. | No nulls allowed | Not a key | Not required | SQ###_SH####. |  |  |  | Trim whitespace, standardize casing, and verify consistency with the sequence and shot portions of shot_id. |
| `department` | VARCHAR | Category text | Production department responsible for the shot’s current or final task. | No nulls allowed | Not a key | Not required | Animation; Assets; Compositing; FX; Lighting; Matchmove; Matte Painting; Roto/Paint. |  |  | The raw variants represent the approved department categories and will be standardized before departmental reporting. | Trim whitespace and map known variants such as Tracking to Matchmove, ROTO to Roto/Paint, and DMP to Matte Painting. |
| `assigned_artist_id` | VARCHAR | Identifier text | Identifier of the primary artist assigned to the production shot. | Conditionally nullable | Foreign key | Not required | ART-###, or null only while a shot is legitimately unassigned. | artists.artist_id |  | Null is permitted only before assignment. Populated IDs remain foreign keys, and missing or orphaned assignments on progressed shots require remediation. | Trim and uppercase IDs, validate against artists.artist_id, preserve null only for legitimately unassigned work, and correct or quarantine missing assignments on progressed shots. |
| `assigned_date` | VARCHAR | Date | Date on which the shot was assigned to its primary artist. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null only while the shot is unassigned. |  |  | The field is logically a date; mixed formats and inappropriate blanks explain the text type and require cleaning. | Parse valid formats, preserve null only for genuinely unassigned shots, and validate assigned_date against assignment and start_date. |
| `start_date` | VARCHAR | Date | Date production work began on the shot. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null only while status is Not Started. |  |  | The field is logically a date. Conditional nulls are valid before work starts; other blanks and malformed values require remediation. | Parse valid formats, preserve null only for not-started work, and correct or quarantine invalid or missing dates on progressed shots. |
| `deadline` | VARCHAR | Date | Required delivery date for the production shot. | No nulls allowed | Not a key | Not required | YYYY-MM-DD and on or after assigned_date/start_date when those dates exist. |  |  | The field is a required date; formatting and date-order defects must be resolved before overdue and on-time-delivery analysis. | Parse valid formats, correct or quarantine the missing value, and validate logical order against assignment, start, and delivery dates. |
| `delivery_date` | VARCHAR | Date | Date on which the shot was delivered. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null until the shot is delivered. |  |  | Nulls are valid only for undelivered shots; mixed formats and impossible date order require cleaning. | Parse valid dates, preserve nulls for undelivered shots, require a date for delivered work, and validate it against start_date and deadline. |
| `first_pass_date` | VARCHAR | Date | Date the shot was first submitted for production review. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null before the first review submission. |  |  | The field is logically a conditional date; malformed values and inappropriate blanks require remediation. | Parse valid dates, preserve null only before first submission, and quarantine invalid or chronologically inconsistent values. |
| `final_approval_date` | VARCHAR | Date | Date the shot received final production approval. | Conditionally nullable | Not a key | Not required | YYYY-MM-DD, or null until final approval. |  |  | Nulls are conditional on workflow status; mixed or invalid source values will be standardized or quarantined. | Parse valid dates, preserve nulls before approval, require a date for approved/final/delivered work as appropriate, and validate chronological order. |
| `status` | VARCHAR | Category text | Current or final workflow stage of the production shot. | No nulls allowed | Not a key | Not required | Not Started; WIP; Pending Review; Client Review; Approved; Final; Delivered; On Hold. |  |  | The source uses multiple labels for the same shot states; standardization is required before status-based KPI analysis. | Trim whitespace and map known variants to the eight approved workflow statuses. |
| `priority` | VARCHAR | Category text | Urgency assigned to the production shot for scheduling and resource decisions. | No nulls allowed | Not a key | Not required | Low; Medium; High; Critical. |  |  | Priority is required for planning; the raw blanks are incomplete descriptive values that need confirmation. | Standardize capitalization and investigate or fill missing priority only from an approved source. |
| `complexity` | VARCHAR | Category text | Estimated production complexity used for planning and workload analysis. | No nulls allowed | Not a key | Not required | Low; Low-Medium; Medium; High; Very High. |  |  | Complexity is expected for planning; raw blanks require confirmation rather than silent imputation. | Standardize to the approved complexity scale and investigate or fill missing values only from an approved source. |
| `estimated_hours` | VARCHAR | Decimal | Planned labor hours required to complete the shot. | No nulls allowed | Not a key | Not required | Positive numeric value. |  | Labor hours | The field is expected to be positive numeric; invalid source strings, missing values, non-positive values, and outliers explain the text type and require remediation. | Convert valid values to decimal, reject non-positive values, investigate outliers, and quarantine unresolved values from estimate-accuracy calculations. |
| `actual_hours` | VARCHAR | Decimal | Reported total labor hours spent on the shot. | Conditionally nullable | Not a key | Not required | Non-negative numeric value; null only before work is recorded. |  | Labor hours | The field is logically numeric and conditionally nullable; malformed, non-positive, missing-on-completed, and extreme values require cleaning and reconciliation. | Convert valid values to decimal, remove text suffixes, preserve null only before work is recorded, reject negatives, investigate zero/outliers, and reconcile against summed time entries. |
| `internal_revision_count` | BIGINT | Integer | Number of internal revision rounds recorded for the shot. | No nulls allowed | Not a key | Not required | Non-negative whole number. |  | Revision rounds |  | Preserve valid non-negative integers and reconcile against internal review-event records. |
| `client_revision_count` | BIGINT | Integer | Number of client-requested revision rounds recorded for the shot. | No nulls allowed | Not a key | Not required | Non-negative whole number. |  | Revision rounds |  | Preserve valid non-negative integers and reconcile against client review-event records. |
| `revision_count_reported` | BIGINT | Integer | Raw total revision count reported by the production tracking export. | No nulls allowed | Not a key | Not required | Non-negative whole number equal to the intended reported total. |  | Revision rounds | The reported count is expected to be a complete non-negative integer; missing, negative, or inconsistent values require reconciliation. | Reject negative values, investigate missing values, and reconcile the reported total against internal_revision_count, client_revision_count, and review events. |
| `hold_days` | BIGINT | Integer | Number of calendar days the shot was blocked or placed on hold. | No nulls allowed | Not a key | Not required | Non-negative whole number. |  | Days |  | Preserve valid non-negative integers and validate consistency with On Hold status and production notes. |
| `vendor_dependencies` | VARCHAR | Category text | External dependency category that may affect completion of the shot. | No nulls allowed | Not a key | Not required | None; 3D Asset; Plate Prep; Client Plate; External Simulation. |  |  | A dependency status is required. Blank values are incomplete because no dependency should be represented explicitly as None. | Trim and standardize categories; investigate blanks rather than treating them as None automatically. |
| `notes` | VARCHAR | Text | Optional free-form production notes describing shot-specific context or constraints. | Nulls allowed | Not a key | Not required | Free-form text, or null when no note is recorded. |  |  |  | Trim populated text, convert empty strings to null, and preserve the original wording. |

## Time Entries (`time_entries`)

**Description:** Contains records for time entries used by the approved project analysis, including shot receiving the time, project receiving the time, and artist logging the time.

**Grain:** One row per logged work entry

**Expected primary key:** `time_entry_id`

**Relationships:**

- time_entries.artist_id → artists.artist_id
- time_entries.project_id → projects.project_id
- time_entries.shot_id → shots.shot_id

| Column | Observed type | Expected type | Definition | Null rule | Key role | Uniqueness | Valid values / format | Relationship | Unit | Notes / exceptions | Cleaning expectation |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `time_entry_id` | VARCHAR | Identifier text | Unique identifier assigned to each logged work or timesheet entry. | No nulls allowed | Primary key | Required | TE-#######: uppercase TE, a hyphen, and seven digits. |  |  | time_entry_id remains the intended primary key. Repeated values are duplicate raw fact rows that must be removed or resolved. | Remove exact duplicate entries, quarantine conflicting repeated IDs, standardize formatting, and verify uniqueness and completeness. |
| `shot_id` | VARCHAR | Identifier text | Identifier of the production shot receiving the logged work time. | No nulls allowed | Foreign key | Not required | PRJ-####_SQ###_SH####. | shots.shot_id |  | shot_id remains a required foreign key. Entries without a resolvable shot cannot safely support shot-level labor analysis. | Trim and uppercase valid IDs, correct only confidently recoverable values, and quarantine blank, placeholder, or unresolved shot references. |
| `project_id` | VARCHAR | Identifier text | Identifier of the project receiving the logged work time. | No nulls allowed | Foreign key | Not required | PRJ-####. | projects.project_id |  | project_id remains a required foreign key and must agree with both the Projects table and the project derived from shot_id. | Trim and uppercase IDs, validate against projects.project_id, reconcile to the project inherited through shot_id, and quarantine unresolved contradictions. |
| `artist_id` | VARCHAR | Identifier text | Identifier of the artist who logged the work time. | No nulls allowed | Foreign key | Not required | ART-###. | artists.artist_id |  | artist_id remains a required foreign key. Entries without a resolvable artist cannot safely support utilization or labor-cost metrics. | Trim and uppercase valid IDs, validate against artists.artist_id, and quarantine blank or unresolved references from utilization and cost analysis. |
| `work_date` | VARCHAR | Date | Calendar date on which the logged work was performed. | No nulls allowed | Not a key | Not required | YYYY-MM-DD. |  |  | The field is logically a required date; malformed source values explain the text type and require cleaning. | Parse valid formats, store as a true date, and quarantine placeholder or invalid values that cannot be corrected reliably. |
| `hours_logged` | VARCHAR | Decimal | Number of labor hours recorded for the individual timesheet entry. | No nulls allowed | Not a key | Not required | Greater than 0 and no more than 24 hours. |  | Hours | The field is expected to be a positive numeric duration not exceeding 24 hours; malformed and out-of-range source values require remediation. | Convert valid values to decimal, remove text suffixes, reject values <= 0 or > 24, and quarantine unresolved entries from hour-based KPIs. |
| `task_type` | VARCHAR | Category text | Category describing the type of work performed during the time entry. | No nulls allowed | Not a key | Not required | Production; Review Fixes; Technical Setup; QC; R&D. |  |  |  | Trim whitespace and standardize to the five approved task-type labels. |
| `billable_flag` | VARCHAR | Boolean | Indicates whether the logged time is billable to the client. | No nulls allowed | Not a key | Not required | Y = billable; N = non-billable. |  |  | The source stores one boolean concept with several text encodings. The cleaned field will use only Y and N. | Standardize affirmative values to Y and negative values to N; investigate unrecognized values. |
| `overtime_flag` | VARCHAR | Boolean | Indicates whether the time entry represents overtime work. | No nulls allowed | Not a key | Not required | Y = overtime; N = regular time. |  |  | The source stores one boolean concept with several text encodings. The cleaned field will use only Y and N. | Standardize overtime-equivalent values to Y and regular-time equivalents to N; investigate unrecognized values. |
| `source_system` | VARCHAR | Category text | Source application or export through which the time entry was recorded. | No nulls allowed | Not a key | Not required | Production Tracker; Timesheet Portal. |  |  |  | Trim whitespace and standardize to the two approved source-system labels. |
| `comment` | VARCHAR | Text | Optional free-form comment providing context about the logged work. | Nulls allowed | Not a key | Not required | Free-form text, or null when no comment is recorded. |  |  | Missing comments are expected; repeated common comments do not by themselves indicate duplicate entries. | Trim populated text, convert empty strings to null, and preserve the original wording. |
