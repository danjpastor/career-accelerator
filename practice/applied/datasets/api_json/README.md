# Local REST API Fixtures

Start with `page_1.json` and follow each `next_page` value.

The fixtures intentionally include:

- a missing optional `tags` field
- a duplicated record ID
- a numeric value encoded as text
- a new `discount` field on the last page
- a simulated 429 rate-limit response

The exercise does not require internet access.
