-- Applied Lab 34: Build a raw-to-analytics data workflow
-- Concepts: raw layer, staging views, clean layer, analytical marts, idempotency, validation, lineage
-- Run with DuckDB. Keep validation queries separate from the main result.

-- Primary source example: read_csv_auto('../datasets/pipeline/raw_orders_2026_01.csv')
-- Adjust the relative path based on your VS Code working directory.

-- 1. State the intended grain and metric definitions.

-- 2. Build staging or analytical logic.

-- 3. Add row-count, uniqueness, null, and reconciliation checks.

-- 4. Write a plain-language interpretation as SQL comments.
