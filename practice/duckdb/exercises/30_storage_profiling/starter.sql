-- DuckDB Exercise 26: Profile tables for analytical storage decisions
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: List the practice tables available in the main schema. Required output: return only these columns in this order: `table_name`. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Profile the row count and distinct business key count for the orders table. Required output: return only these columns in this order: `row_count`, `distinct_order_ids`. A correct result contains 1 row. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Compare row count with distinct order IDs in the order-items table to describe its grain. Required output: return only these columns in this order: `row_count`, `distinct_order_ids`. A correct result contains 1 row. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Create a compact table profile showing table name, row count, and stated analytical grain for three selected tables. Required output: return only these columns in this order: `table_name`, `row_count`, `grain`. A correct result contains 3 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

