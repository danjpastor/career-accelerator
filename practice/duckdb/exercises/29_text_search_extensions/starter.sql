-- DuckDB Exercise 25: Search text safely and inspect DuckDB extensions
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Find tickets whose customer name contains the word mart without depending on letter case. Required output: return only these columns in this order: `ticket_id`, `customer_name`. A correct result contains 2 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Find feedback issue values that contain billing after trimming and ignoring case. Required output: return only these columns in this order: `response_id`, `issue_type_raw`. A correct result contains 6 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Use a regular expression to return feedback rows with a four-digit year in the submitted date text. Required output: return only these columns in this order: `response_id`, `submitted_at_raw`. A correct result contains 17 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Inspect the extensions known to DuckDB and return their name and loaded status. Required output: return only these columns in this order: `extension_name`, `loaded`. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

