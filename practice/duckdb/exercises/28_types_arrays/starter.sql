-- DuckDB Exercise 20: Inspect data types and work with list values
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Inspect the source columns and return safe numeric versions of rating and response minutes. Required output: return only these columns in this order: `response_id`, `rating`, `response_minutes`. A correct result contains 18 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Create a list of issue types for each cleaned channel. Required output: return only these columns in this order: `channel`, `issue_types`. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Expand the list values back into one row per channel and issue type. Required output: return only these columns in this order: `channel`, `issue_type`. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Calculate an average response time using only values that can be converted safely. Required output: return only these columns in this order: `average_response_minutes`. A correct result contains 1 row. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

