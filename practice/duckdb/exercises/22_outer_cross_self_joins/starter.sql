-- DuckDB Exercise 07: Use outer, cross, and self joins
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Use a left join to show every customer and any related orders, keeping customers with no order. Required output: return only these columns in this order: `customer_id`, `customer_name`, `order_id`, `order_total`. A correct result contains 15 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Use a full join to identify customer IDs that appear on only one side of the customer-order relationship. Required output: return only these columns in this order: `customer_id`, `customer_name`, `order_id`, `relationship_status`. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Use a self join to show each employee beside their manager name when one exists. Required output: return only these columns in this order: `employee_id`, `employee_name`, `manager_name`. A correct result contains 12 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Use a cross join to build every combination of department and two named planning scenarios. Required output: return only these columns in this order: `department_name`, `scenario`. A correct result contains 8 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

