-- DuckDB Exercise 03: Filter orders by ranges, patterns, and missing values
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Return the distinct ticket statuses in alphabetical order. Required output: return only these columns in this order: `status`. A correct result contains 3 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Return tickets whose priority is High or Urgent and whose status is not Closed. Required output: return only these columns in this order: `ticket_id`, `priority`, `status`. A correct result contains 5 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Return tickets whose resolution hours are missing. Required output: return only these columns in this order: `ticket_id`, `status`, `resolution_hours`. A correct result contains 9 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Return feedback rows whose raw channel contains the word email after ignoring case and extra spaces. Required output: return only these columns in this order: `response_id`, `channel_raw`. A correct result contains 6 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

