-- DuckDB Exercise 10: Use subqueries in filters, sources, and calculations
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Use a subquery in WHERE to return retail orders whose revenue is above the overall average. Required output: return only these columns in this order: `order_id`, `region`, `revenue`. A correct result contains 9 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Use a subquery in FROM to calculate total item revenue per order and then return the summarized rows. Required output: return only these columns in this order: `order_id`, `order_revenue`. A correct result contains 10 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Use a scalar subquery in SELECT to show each retail order revenue beside the overall average revenue. Required output: return only these columns in this order: `order_id`, `revenue`, `overall_average_revenue`. A correct result contains 24 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Use a nested or correlated subquery to return products that appear in at least one order item. Required output: return only these columns in this order: `product_id`, `product_name`. A correct result contains 8 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

