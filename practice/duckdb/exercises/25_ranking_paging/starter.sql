-- DuckDB Exercise 15: Rank results and select top records
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Rank all employees from highest to lowest salary using a ranking function that preserves ties. Required output: return only these columns in this order: `employee_id`, `employee_name`, `annual_salary`, `salary_rank`. A correct result contains 12 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Rank employees by salary inside each department. Required output: return only these columns in this order: `employee_id`, `department_id`, `annual_salary`, `department_rank`. A correct result contains 12 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Return the top two salaries in each department using a window rank. Required output: return only these columns in this order: `department_id`, `employee_name`, `annual_salary`, `department_rank`. A correct result contains 8 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Number tickets in opening order for each support agent. Required output: return only these columns in this order: `agent_id`, `ticket_id`, `opened_at`, `ticket_sequence`. A correct result contains 18 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

