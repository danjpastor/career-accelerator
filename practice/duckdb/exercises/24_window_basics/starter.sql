-- DuckDB Exercise 14: Add row-level context with window functions
-- Read README.md before starting.
-- Save your completed copy under practice/duckdb/submissions/


-- -----------------------------------------------------------------
-- Q1. Task: Return every order with the overall average revenue added as a window value. Required output: return only these columns in this order: `order_id`, `revenue`, `overall_average_revenue`. A correct result contains 24 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q2. Task: Return every order with the average revenue for its region. Required output: return only these columns in this order: `order_id`, `region`, `revenue`, `regional_average_revenue`. A correct result contains 24 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q3. Task: Return each order with its region total while preserving one row per order. Required output: return only these columns in this order: `order_id`, `region`, `revenue`, `regional_revenue`. A correct result contains 24 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------


-- -----------------------------------------------------------------
-- Q4. Task: Return each order with its percentage contribution to regional revenue. Required output: return only these columns in this order: `order_id`, `region`, `revenue`, `regional_share`. A correct result contains 24 rows. Do not include extra columns; keep every filter and sort rule stated in the task.
-- Write and run your query below this comment.
-- -----------------------------------------------------------------

